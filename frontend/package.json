{
  "name": "openutilitybill-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "OpenUtilityBill frontend client (Vite + TypeScript + Bootstrap)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
